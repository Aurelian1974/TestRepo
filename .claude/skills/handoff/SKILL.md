---
name: handoff
description: Checkpoint the current work so it can continue in the other tool (Copilot ⇄ Claude Code) or a later session without chat history. Manual command.
disable-model-invocation: true
---
Execute skill `orchestration` §6 exactly. Output only the `HANDOFF` line.
