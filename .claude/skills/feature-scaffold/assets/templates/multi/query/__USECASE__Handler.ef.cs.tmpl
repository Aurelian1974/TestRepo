using Microsoft.EntityFrameworkCore;

namespace __NS__;

internal sealed class __USECASE__Handler(__DBCTX__ db)
{
    public async Task<__USECASE__Response> Handle(__USECASE__Query query, CancellationToken ct)
    {
        // TODO(ai): AsNoTracking().Where(...).Select(x => new __USECASE__Response.Row(...)); never materialize entities
        var rows = await Task.FromResult(new List<__USECASE__Response.Row>());
        return new(rows);
    }
}
