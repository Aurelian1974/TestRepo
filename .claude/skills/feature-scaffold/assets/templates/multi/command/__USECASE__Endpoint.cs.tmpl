namespace __NS__;

internal sealed class __USECASE__Endpoint : IEndpoint
{
    public void Map(IEndpointRouteBuilder app) =>
        app.MapPost("/__ROUTE__", async (__USECASE__Command command, __USECASE__Handler handler, CancellationToken ct) =>
                (await handler.Handle(command, ct)).ToHttpResult())
           .AddEndpointFilter<ValidationFilter<__USECASE__Command>>()
           .RequireAuthorization("__POLICY__")
           .WithName("__USECASE__");
}
