using FluentValidation;

namespace __NS__;

internal sealed class __USECASE__Validator : AbstractValidator<__USECASE__Command>
{
    public __USECASE__Validator()
    {
        // TODO(ai): shape rules only (required, length, format). Business rules belong to RULE placement.
    }
}
